# Copy to .env and fill in. .env is gitignored and must never be committed.
# On HF Spaces these come from Space secrets instead; do not ship a .env there.

# Sarvam AI - speech to text (required by task requirement #1)
# https://dashboard.sarvam.ai  ->  API Keys
SARVAM_API_KEY=

# Generator. Anthropic by default; see src/generation/generator.py.
ANTHROPIC_API_KEY=

# Optional: only needed to push the Space or pull gated artifacts.
HF_TOKEN=
